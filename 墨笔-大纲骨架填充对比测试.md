# 墨笔 · 逐章大纲「骨架→填充」vs「一次性」对比测试

> 配套改造见《墨笔-改进记录与架构.md》#23 / #23b。本文件专门记录两种 `outline.refine`
> 生成模式的实测对照,供效果对比与后续调参追溯。

## 一、被测对象

`outline.refine` 把"一个 phase(剧情走向)"细化成"逐章可写大纲"。两种模式:

- **oneshot(一次性)**:一次 LLM 调用产出整段 phase 的全部章节(原行为,`max_tokens=12000`)。
- **stepwise(骨架→填充)**:
  - 遍 1「骨架」:一次调用产整段轻量逐章骨架(index/title/intent/beat + 认领 key_event/伏笔,`outline.skeleton`,`max_tokens=6000`)。
  - 遍 2「填充」:逐章展开完整字段(`outline.flesh`,每章 `max_tokens=4000`),每步喂入 骨架全貌 + 前序已定章节钩子 + 待覆盖 key_event 预算管家。

衡量指标:`must_include` 平均条数/章、`must_include+key_events` 字符密度/章、`ending_hook` 覆盖、`must_avoid` 覆盖;并辅以逐章主观审读(连贯性/重复/套路化)。

---

## 二、测试一:短 phase(5 章)

- 书:《光明皇帝·业火》
- 来源:ArcRun #2 winner(候选 #1「明尊弃天」)phase3「新人间界」
- 范围:第 63–67 章(用 override 截成 5 章控额度)
- 各模式跑 1 次。

### 量化对比

| 指标 | oneshot | stepwise |
|---|---|---|
| 章数 | 5 | 5 |
| must_include 均(条/章) | **3.6** | 2.8 |
| key_events 均(条/章) | 5.4 | 5.4 |
| 字符密度(must_include+key_events,字/章) | **183** | 144 |
| 有 ending_hook 的章 | 5/5 | 5/5 |
| 有 must_avoid 的章 | 5/5 | 0/5 |
| 耗时 | 47s | 247s(约 5×) |
| 调用次数 | 1 | 6(1 骨架 + 5 填充) |

### 主观审读

- **oneshot 更好**:5 章各司其职、伏笔回收分散且具体——ch63 苏秋炎圆寂+大相国现身、ch64 风红噩梦+叶蓉重逢+茶灵、ch65 十二宝树王身份揭示、ch66 泰山重铸、ch67 收束。每章 must_avoid 都明确(防剧透)。
- **stepwise 反而更薄、更套路**:beat 扎堆在"泰山/重铸"(ch63 与 ch65 都在铺/做重铸);ending_hook 连续三章公式化——ch63「最后一块拼图,还在等着我们」、ch64「最后的仪式,该开始了」、ch65「新人间界,从此有了根」,且连着三章"大道转身/回首说…";must_avoid 全空。

### 结论与辨析

**本样本 stepwise 未胜,反而略差。** 但关键是**测试落在了错误区间**:

- 5 章时 oneshot 的 12k 输出额度**根本没被摊薄**(≈2400 token/章,绰绰有余),所以 stepwise 想解决的"额度被摊薄→每章写薄"这个病在 5 章**不存在**;
- 反而 stepwise 被"骨架强约束 + 逐章公式化承接"拖累,且**骨架质量成了天花板**(本次骨架把重铸 beat 扎堆,填充只能照做)。

→ 推论:stepwise 的收益应只在 **10–15 章的长 phase** 才显现(那时 oneshot 每章只剩几百 token、才真正写薄)。短 phase 用 oneshot 更好。这与前端开关旁的提示("长 phase 用骨架+填充、短 phase 用一次性")一致。

---

## 三、测试二:长 phase(13 章)

- 书:《光明皇帝·业火》
- 来源:ArcRun #2 winner(候选 #1)phase0「残魂泣血」
- 范围:第 26–38 章(完整 13 章,**不截断**——这正是 stepwise 理论上该发力的长 phase 区间)
- 各模式跑 1 次。期间修了两个 stepwise 健壮性 bug:骨架 JSON-in-text 偶发判空(加重试 2 次)、`key_events`/`must_include` 偶含 dict 元素致 join 崩(`_str_items` 统一转字符串)。

### 量化对比

| 指标 | oneshot | stepwise |
|---|---|---|
| 章数 | 13 | 13 |
| must_include 均(条/章) | **≈3.5** | 2.2 |
| key_events 均(条/章) | **≈4.5** | 3.8 |
| 字符密度(字/章) | **180–193** | 129 |
| 有 ending_hook 的章 | **13/13** | 8/13 |
| 有 must_avoid 的章 | **13/13** | 8/13 |
| 耗时 | ~95–108s | **732s(约 12 分钟,7×)** |
| 调用次数 | 1 | ~15(骨架±重试 + 13 填充) |
| 成本 | (计价表未覆盖,显示 0) | $0.0336 |

### 主观审读

- **oneshot 仍明显更好**:13 章各司其职,伏笔回收致密且具体(每章列 `foreshadow_ids_addressed`、点名实体、`must_avoid` 明确防剧透),叙事线索(叶羽逃亡 / 谢童离宫 / 魏枯雪探昆仑 / 方忏轩现身 / 七百年前真相)交织推进。
- **stepwise 仍更薄、更套路**:部分章只剩 1 条 must_include(如 ch28、ch29);**5/13 章直接没产出 ending_hook**;hook 出现"魏枯雪望着北方夜空…喃喃自语"这类重复句式;`must_avoid` 多数章缺失。

### 结论:**stepwise 在其目标区间(长 phase)依然完败**

这推翻了最初的假设(包括我自己的分析)。复盘真正原因:

1. **"12k 额度被摊薄"这个前提本身不成立。** 大纲是**结构化 JSON**(不是散文),13 章一次产出每章仍有 ~920 token 预算,绰绰有余——实测 oneshot 13 章 char 密度照样 180+、must_include 3.5 条。我们为"额度不够"设计的逐章方案,治的是一个不存在的病。
2. **骨架预先把每章"钉"成一个薄 beat,填充只能照做** → 失去了 oneshot"看到全 phase、自由地把伏笔密集编织进各章"的优势。
3. **填充模型更省字**:单章框架让它倾向只写 2 条 must_include、且常漏掉 ending_hook/must_avoid。
4. 代价是 **7× 耗时、10×+ 成本、~15× 调用**(火山额度敏感)。

### 最终建议

- **oneshot 维持默认**,且在本模型(doubao-seed)+ 本类书上,短 phase、长 phase 都更优。
- **stepwise 保留**(已在开关后、可安全降级、不影响默认路径),但**当前不推荐常规使用**——它的理论收益在 doubao 上没有兑现。
- 若要让 stepwise 翻盘,方向是**调填充提示**(逼 ≥4 条具体 must_include、强制产 ending_hook、禁套路 hook、要求引用 foreshadow id),而非维持现状;留作后续实验,本次不动以保对照纯净。
- 真正"细节不够/矛盾"若仍出现,更可能的杠杆在 **oneshot 提示与 max_tokens**(而非拆成逐章),以及 arc 骨架质量(phase 概要本身)。
