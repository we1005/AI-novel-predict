# 墨笔 (MoBi) · 写作 Agent 改进记录与架构文档

> 本文沉淀这一轮深度打磨的全过程：架构、各模块、踩过的坑与修复、模型路由规律、续写工作流模式、验证结论。便于后续展示与维护。

## 一、系统总览

中文长篇小说**续写** multi-agent 工具。后端 FastAPI + SQLite（每本书一个 `data/books/<书名>/novel.db`），前端 Next.js。LLM 走 OpenAI 兼容接口，支持**多服务商**（阿里 DashScope / 火山引擎 Coding-Plan）。

主链路：
```
语料切分 → 6-agent 抽取(实体/伏笔/状态/剧情/世界规则/疑点)
        → [可选] 文笔风格分析
        → 剧情预测(predict 单章 / arc 全弧) → 评分选优
        → 大纲(逐章) → 成稿(Writer + 文风/剧情/一致性三审 + Editor 门控)
        → [可选] 双语 / 重写文笔
```

## 二、模型路由规律（本轮最重要的工程结论）

火山引擎模型分两类，**必须按任务类型路由**：

| 任务类型 | 用模型 | 原因 |
|---|---|---|
| 结构化 / 工具 JSON 输出（抽取、评分、骨架、去重、文风分析、预测候选、大纲） | **doubao-seed-2.0-code** | 代码模型 JSON 干净；minimax/glm/kimi 在强制 tool_choice 下会把输出吞进 reasoning 通道，产出空/坏 JSON |
| 散文写作（成稿、双语、重写、翻译、融合） | **minimax-m3** | 盲测文笔最佳，且"少废话直接出答案"，不易被 reasoning 吃光预算 |
| 审查（便宜快、量大） | **doubao-seed-2.0-lite** | 成本低，工具调用稳定 |

**反面教材**：glm-5.2 / kimi-k2.6 / doubao-pro 是重推理模型，在"长输入+长输出"任务（如双语融合）上会把 token 预算几乎全花在隐藏 reasoning 上 → 正文为空。

## 三、踩过的坑与修复（闭环验证发现，"只构建不验证"绝对发现不了）

1. **文风审查横跳**：每稿在"太华丽/太碎"间反复，全标"翻译腔"→ 耗尽返工。
   修复：审查锚定原文风格片段，不强加偏好；**确定性门控：文风永不触发返工**，只有剧情/一致性硬伤才返工，并保留最干净一稿。
2. **predict/arc/outline 在 minimax-m3 上整条死掉**：强制工具调用产出坏 JSON → 0 候选。
   修复：① 客户端用 json_repair 兜底修复工具参数；② 把 5 个强制工具 agent 路由到 doubao-code。
3. **winner_index 偶发 null** → outline 崩溃。修复：`_ensure_winner` 取综合最高分兜底。
4. **doubao 推理模型在强制工具下吞输出**（骨架抽取空）→ 改 **JSON-in-text** 解析。
5. **双语融合超长 4 段输入(3.1万字)空输出** → **拆成中/英两次小融合**（各 ~2万字），并行。
6. **空输出偶发（连 minimax 散文也偶发）** → **客户端层空输出自动重试**（无正文且无工具调用即重试，最多3次），全 agent 通用保险。
7. **merge max_tokens（多模型扫描定值）**：扫描 glm-5.2/kimi-k2.6/minimax-m3 在 8k/16k/32k 的表现，发现**所有推理模型的隐藏 reasoning 长度极不稳定**（minimax-m3 同一任务 reasoning 在 5k–66k 字之间跳；8k→空，16k→截断，32k→完整）。结论：merge 用 **minimax-m3 @ max_tokens=32000**；并加**长度兜底**——若融合稿为空或不足源文 40%（被截断），回退到完整的独立稿。配合"空输出自动重试"，双保险。250000 这种值超出模型输出上限、无意义。
8. **图谱/宝物功法对复杂书是垃圾**：① 多批抽取把同一实体拆成多名字（西泽尔/西泽尔·博尔吉亚）→ **LLM 判定的实体去重合并**（FK 全量重指向）；② 重要度重算；③ 关系抽取填充；④ 宝物页"主角库存"模型不适用机甲书 → **目录兜底**（按重要度+伏笔展示全书物品）；⑤ 多视角横幅提示 + 一键「整理图谱」。
9. **语料进度 5/4·125% bug**：进度条denominator 用"章节数÷输入框批大小"估算，与真实批次不符 → 改为**覆盖率(已抽取章节÷总章节)**驱动，钳到 100%。
10. **bilingual.en_writer 静默走错模型**（并行评审 pass 发现）：英文母语稿调用 `model=MODEL_STRONG`（env 默认 qwen3.5-flash）且**该 agent 未注册进 AGENT_REGISTRY** → `apply_overrides` 对未注册 agent 直接放行传入的 model → 英文稿用 qwen3.5-flash 而非散文道 minimax-m3，与"地道英文技法"目标相悖。修复：把 `bilingual.en_writer` + `revoice.write.{en,mimic,wangwen}` 注册进「文笔」组 STRONG 道（无 override → minimax-m3）；`revoice.skeleton` 也注册并在 settings.json **钉死 doubao-code**（注意：注册进 STRONG 道后，若不钉 override 会被 lane 默认 minimax-m3 覆盖，反而冲掉显式传入的 doubao-code——这是 registry lane 优先级的坑）。
11. **双语轮询可能无限轮询**（前端 review 发现）：`genBilingual` 的 `setInterval` 仅在 status≠writing 时清除，catch 吞错 → 后端卡死即每 5s 永久轮询。修复：加 96 次（~8 分钟）上限 + 超时提示，自终止。
12. **图谱页硬编码 `localhost:8000` 裸 fetch**：绕过 `api` 封装与 BASE 变量、无错误处理 → 部署到非本地即断。修复：`graphCharacters(topN)`/`timeline(minImp)` 封装支持查询参数，页面改走封装。
13. **双语 translate 阶段 max_tokens 太小 → 整个 job 失败**（本轮闭环跑真实 job 才暴露！）：`translate.en2zh` 翻译一篇顶满 8000 token 的英文稿，minimax-m3 隐藏 reasoning 吃光 9000 预算 → `finish=length`、正文空，客户端重试 3 次仍空 → 异常上抛、**整章双语 job 直接 failed**（连"长度兜底"都没机会跑，因为异常发生在 merge 之前）。这是第 7 点 merge 结论在 translate/draft 阶段的重演。修复三连：① draft 8000→16000、translate 9000→**20000**，给 minimax-m3 reasoning 留足头寸；② `_translate` 包 try/except，失败返回 ""（**非致命**，不再拖垮整个 job）；③ merge 改为 `_merge_or_keep`——只有原稿+互译稿都在才融合，某语种互译为空就直接保留独立稿。**教训：只有真的端到端跑一遍真实 job 才会暴露这种"某一阶段在长输入下被 reasoning 吃光预算"的连锁失败；模型解析层验证（model 路由对不对）远远不够。**
14. **predict/arc 在大上下文下整条死掉——forced tool_choice 静默吞输出**（端到端跑预测才暴露）：predict.diverge / arc.diverge 用 doubao-code + **强制 tool_choice**，但 stage-A 的系统上下文高达 **~8.9 万字**（实体表+世界规则+伏笔+疑点+近章+新增的叙事基因块）。doubao/volc 推理模型在这种大上下文下会返回 `finish_reason=tool_calls` 但 **tool_calls 数组为空、content 也空**（completion_tokens 明明有 1900+，但输出既不在 content 也不在 tool_call 里，被 volc 的工具序列化吞了）→ 0 候选 → 加上 #6 的空输出守卫，重试 3 次仍空 → **predict 直接 500**。**根因**：第 4 点（骨架抽取）那个"doubao 强制工具吞输出"的坑，在 predict/arc 这种更大上下文下必现。**修复**：predict.diverge/score、arc.diverge/score 全部从 forced tool_choice 改为 **JSON-in-text**（系统里贴严格 JSON 结构/Schema，不带 tools，`json_repair` 解析）。同样 88k 上下文下实测：forced-tool→0 候选；JSON-in-text→干净 3 候选。**且确认预测确实文风感知**：winner_reason 明确写"完全符合原作者叙事基因（单核心主角视角、政治博弈悬念…）"。**模型路由结论修订**：doubao-code 仍是结构化输出首选，但**机制要用 JSON-in-text 而非 forced tool_choice**——尤其上下文大时；forced tool_choice 只在小上下文勉强可用，不可依赖。**附带**：JSON-in-text 下 arc.score 偶发 winner_index=null（与第 3 点 predict 同病），新增 `_ensure_arc_winner` 兜底（取综合分最高的弧），防止 ArcRun.chosen_index=None 拖垮 outline.refine。arc 实测：2 弧、各 5 core_truths/5 phases、winner 兜底有效。**同链路 outline.refine 同病同治**：它也吃同一份 ~89k `_ctx_blocks`，同样从 forced tool_choice 改 JSON-in-text（贴 schema，复用已有 json_repair 兜底）。**跨书回归**：末法王座（长网文、无文风画像、上下文不含叙事基因块）predict 同样 JSON-in-text 通过（3 候选）；故该修复对"有/无文风画像"两类书都成立。**全量排查其余 forced tool_choice 点**：ingest 抽取、style.analyze、graph.dedup、draft 三审/editor、relationships、sim —— 其中 draft 链路本轮 ch159 一稿 approve 已证可用；ingest/style.analyze 此前整本跑通；这些上下文都没到 89k 量级，暂不动；**经验法则：凡是吃 `_ctx_blocks` 全量上下文的结构化调用，一律 JSON-in-text，不要 forced tool_choice。**
15. **ingest/sim 异常路径 null-deref + 状态卡死**（第二轮并行评审发现）：`extract.py` 异常处理里 `b = s.get(ExtractionBatch, batch_id); b.status=...` 若 b 为 None 会**在异常处理里再抛异常**、盖掉原始错误，且批次卡在 "running" → `run_all` 永久跳过该区间死锁。修复：done/failed 两处都加 `if b:` 守卫。`simulator.py` 三处状态更新（rounds/reporting/done）同样 null-deref，加 `if row:`。前端 `interviewStream` 硬编码 localhost → 改 BASE。**驳回误报**：ingest 的 books 带 .active、predictWrite 本就用 BASE、predict/arc/sim 前端守卫已够、settings 页干净。**抽取大上下文韧性兜底（本轮已加固）**：`_agent_call`（6 个抽取 agent 共用）原是裸 forced tool_choice，超大新书后期批次实体表上下文可能破 89k 触发空工具失败。改为**先 forced-tool（happy path 不变，小书首试即过）→ 若返回空 tool_use 或客户端空输出守卫抛错 → 自动回退 JSON-in-text（贴 schema、json_repair 解析）**。零 happy-path 影响（小上下文实测仍首试返回 4 实体 林轩/老王/小雨/铁匠铺），仅在大上下文失败时多花几次重试后由兜底救回。抽取 agent 实为混合模型（entity/foreshadow→qwen3.5-flash、state→doubao-lite），故此前未必命中 doubao-code 那个 bug，但兜底对所有模型通用。**通则**：凡结构化调用吃全量 `_ctx_blocks`，一律走 JSON-in-text（或带 JSON-in-text 兜底）。

16. **双语中英稿"各写各的"——讲成了两个不同的故事**（裁决真实成品才发现，最严重）：原设计是"独立中稿 + 独立英稿 → 交叉互译 → 取长补短"。但 `_en_draft` 用 `EN_WRITER_SYSTEM`、**既无文风画像也无世界观/人物锚点**，只给一句 brief + 1200 字中文 tail → 英文稿凭空编了套沙漠+铁轨+Marl Voss 的设定，而中稿是正确的西泽尔/翡冷翠/机械神教（文艺复兴意大利蒸朋）。两种语言根本不是同一章的两个版本，而是两个无关的故事——"取长补短融合"的前提彻底崩塌。**根因**：独立并行起草，brief 一松两边就各自发散；英文侧尤其没拿到 `setting_register`（翡冷翠=Florence、博尔吉亚=Borgia、各机甲专名）。**修复（改架构）**：① 新增 `continuation_setting()` 暴露世界观+专名体系；② 英文稿改为**以中文稿为蓝本的"母语英文再创作"**（同人物/同设定/同节拍，把中译名还原成西方本名 西泽尔→Cesare 等；是地道英文重述、**非逐字翻译**——仍用英文技法冲淡西幻腔，但不再发散）；③ stage1 由并行改**串行**（中→英），从根上杜绝跑题。**疑点**：早先 #32 的 ch157 成品很可能也有同样的"各写各的"问题，只是当时没逐字裁决中英是否同一个故事——已记，后续若用得到那份成品需复核。**教训：成品不能只看"非空/够长/能跑完"，必须真读内容、裁决中英是否真的是同一章。**

> 评审方法论：用两个只读 Explore agent 分别扫前后端"找新问题"，逐条**复核再修**——本轮驳回两个误报（ingest 的 books 数组其实带 `.active` 字段；registry 的 max_tokens 不会覆盖调用层的 32000，只有 settings.json override 才会）。"评审 agent 提的不一定对"，必须自己核。

> 两点非 bug 但需知晓：① 火山 coding-plan 是**包量/额度计费**，故 `PRICE_PER_MTOK` 里所有火山模型价=0，`cost_usd` 恒 0 是有意为之（不是统计漏算）。② minimax-m3 单次出稿慢（en_writer 8000 tokens 约 160s），双语整章墙钟约 6-7 分钟（stage 内并行后 ≈ max(zh,en)+max(2译)+max(2融)）；前端轮询上限相应设到 12 分钟。

## 四、文笔风格 / 续写四种模式

详见 `续写工作流设计.md`。一套可组合引擎 + 按书开关（非硬编码多管线）：
- **长篇网文（弃用文风）**：mimic 关 → 明快网文腔。
- **传统中长篇 / 仿写**：mimic 开 → 注入作者文风画像（整体文风+分场景笔法+叙事节奏+套路+须避免）。
- **双语（西方背景）**：独立中/英稿 → 交叉互译 → 取长补短融合，用英文技法冲淡廉价西幻腔。
- **重写文笔保主干（re-voice）**：抽剧情骨架 → 用目标文笔重写，剧情/关键信息/钩子不变；支持原著章节与**已生成新章节**。

预测模块已**文风/叙事感知**：把分析出的叙事结构/节奏/套路注入 predict & arc 上下文，mimic 书按作者编排习惯预测。

## 五、验证结论（在真实书上裁决）

- **末法王座**（长篇网文，1472 章）：续写 28 章(1473–1500，~12万字)全 approve，网文腔；predict/draft 兼容，无回归。导出 txt + docx。
- **龙族**（短篇 80 章）：predict 可用。
- **天之炽**（156 章，西方蒸朋/多视角/插叙）：全流程 predict→大纲→成稿(仿写一稿过) + 中英双语(ch157，英文母语地道、中文保原作意象) + 重写文笔(ch30 网文化，8/8 关键信息保留)；图谱 40 节点/264 边/77 关系，宝物 98 件目录。

四种模式 + 多服务商 + 空输出重试均**加法式、按书 opt-in、零回归**。

### 龙族 全流程重跑(用户要求"从抽取开始所有流程重来"，逐阶段跟踪+裁决)
清空龙族派生数据后端到端重跑,每阶段裁决,又暴露两个真实健壮性 bug:
- **17. dedup 在 dict 形状别名上崩溃(500)**:抽取模型偶尔把元数据对象塞进 `entity.aliases_json`(实体「蜘蛛切」得到 `{first_appear_chapter,description,match_existing_id}`),`_candidates` 假定别名是字符串 → `TypeError: got 'dict'`。修复:新增 `_alias_strs()` 把任意形状别名拍平成字符串(dict 提 name/alias/title/value、其余跳过)+ `_norm()` 防御非字符串。注意:ORM 的 JSON 列会反序列化,裸 SQL 看到的是字符串、ORM 看到的是 dict——必须按 ORM 视角排查。
- **18. style.analyze forced tool_choice 大上下文吞输出**:与第 14 点同病。doubao-code 在龙族(单章均 2 万字、文风 schema 嵌套很深)下返回 `finish=tool_calls` 但工具数组空+正文空 → 客户端空守卫 → 400。修复:改 JSON-in-text(贴 STYLE_TOOL schema,复用已有 `_loads(resp.text)` 兜底)。**通则再次验证:凡结构化大上下文调用一律 JSON-in-text,forced tool_choice 不可靠。**(天之炽章短没触发,龙族章长必现——再次印证"只有真跑不同的书才暴露"。)

龙族重跑裁决结论(全部高质量):抽取 250 实体(人物准确:路明非/楚子航/源稚生/上杉绘梨衣/路鸣泽);图谱去重 255→250、13 条关系教科书级准确(路明非—缔约魔鬼宿主—路鸣泽、源稚生—反目兄弟—源稚女)、重要度路明非#1;文笔画像精准(江南都市青春史诗+暗黑幻想腔、双生宿命/魔鬼交易套路、衰仔/黄金瞳/皇 标志词);predict 3 候选(伏笔锚定、叙事基因感知);arc 2 弧(黑王之卵/奥丁之渊,各 5-6 真相/5 阶段);outline 8 章(冰海漂泊的迈巴赫…);draft ch81 一稿 approve(地道龙族腔:芬格尔吐槽/路明非衰仔内心戏/村雨/楚子航失踪线)。交付物 `龙族_续写_ch81_仿写_verified.txt`。

### 整本故事弧推演(whole-book projection · 新功能)
目标:把后续整本书剧情一次推演到结局。**策略裁决**:纯"递归下10章+问大模型该不该停"会漂移且收束不了伏笔;纯"一次生成整本大纲"在 tail 退化(实测天之炽旧 arc:phase3 ch206→199 倒挂、arc2 只覆盖估算的一半)。采用**有界递归混合架构**:
1. **arc 顶层骨架定全局**:core_truths(关键问题)+ 每阶段伏笔收束计划 + 高潮 + 结局 + 体量估算 → 天然 bound 终点,不靠脆弱的"该停了吗"判断。
2. **`_sanitize_phases` 重锚定**:把 arc 的 phase 范围修成从 当前章+1 起、连续单调(修 end<start/重叠/缺口),每 phase clamp 到 [4,24] 章。
3. **逐阶段展开 + 章号重锚**:每 phase 走 `outline.refine`(加 `chapter_start/end` override + 上一阶段结尾 `continuity_hint` + `persist=False`),聚合后**章号连续重排**——漂移不再跨阶段累积。
4. **完整性裁决**(doubao-code, JSON-in-text):core_truths 是否都揭晓、主要伏笔是否收束、节奏断层,产 needs_work/pass + 缺口清单。
- 新表 `story_projection`(append-only)+ 后台 job + stage 进度;端点 `POST /predict/arc/runs/{id}/project`、`GET /predict/projections[/{id}]`;前端 arc 页 `WholeBookPanel`「🌌 推演整本书」(选候选→推演→体量+裁决+按阶段分组的全书大纲)。
- **天之炽实测**:arc#0(龙战于野·梵天陨落,6阶段)→ 推演出 **ch157-227 共 71 章**全书大纲,**章号 0 缺口连续**(验证重锚定彻底解决了 raw arc 的 tail 倒挂),抵达结局(225 梵天陨落倒计时→227 红莲绽放)。裁决 needs_work:6/6 关键问题揭晓、主伏笔收束,但诚实标出 3 条游离伏笔 + 真实问题(早期版本 ch158 标题残留"第169章"前缀 → 修:重锚定时 `_clean_title` 剥离残留章号、`_str_list` 兼容 dict 形状的 must_include)。交付物 `天之炽_整本故事弧推演_ch157-227.txt`。
- **回答用户的策略疑问**:递归是对的机制但要被骨架约束、停点由骨架钦定;"初始大纲必须很好"的风险用**消毒+重锚定+裁决**从结构兜底,而非赌一次生成完美。

### 角色仿真模块审查+优化+端到端验证(用户要求"尝试下和审查下")
- **19. profile.build / sim.decide forced tool_choice 静默吞输出**:两个 agent 都跑 doubao-lite(火山推理),都用 forced tool_choice。档案会悄悄构建失败、仿真里角色动作凭空消失(sim.decide 之前还**没有任何兜底**)。与 #14/#18 同病。修复:都改 JSON-in-text(贴 schema、去 tools、json_repair 解析正文)。
- **端到端实测(天之炽)**:`character_profiles` 构建 **20/20 成功**(西泽尔=教皇私生子红龙、佛朗哥=密涅瓦总长、龙德施泰特=炽天骑士团长,准确);仿真 5 角色 × 3 轮**每轮 5 个动作全部产出**(修复前 doubao-lite forced-tool 会丢动作),角色决策在线且相互勾连(西泽尔↔阿黛尔联姻线、佛朗哥喊西泽尔做神经耦合测试);ReportAgent(minimax-m3)综合出连贯涌现章节《第157章 暗流交汇》(多视角、江南腔、设定正确)。交付物 `天之炽_角色仿真_ch157_暗流交汇.txt`。**至此 forced-tool→JSON-in-text 已覆盖全部结构化 agent:抽取/predict/arc/outline/style/dedup判定/profile/decide。**

### 前端交互优化(本轮)
- **伏笔甘特图默认折叠 + 卡片化**:309 行甘特图视觉轰炸 → 甘特图默认折叠(展开才渲染 ECharts),默认改为响应式卡片网格(类型彩签+状态签+章节跨度+描述+收束),加文字搜索框,状态/类型筛选对卡片与甘特图同时生效。
- **逐章成稿"章节时间线"未按大纲过滤**:底部时间线/历史成稿渲染全书 `drafts`,切大纲不联动 → 新增 `shownDrafts` 按 `outline_run_id` 过滤,两套主题都改用,空态文案更准。

### 用户报障修复
- **历史预测无法查看**（用户截图报障）：剧情预测·章 页的「历史预测」表只列不可点——`<tr>` 没有 onClick，没有任何查看入口（全弧页有「查看」按钮，章页漏了）。修复：行可点 → `predictGet(id)` → 载入候选+评分到既有详情区（光标/悬停/选中高亮+提示行+回顶）；并对早期 0 候选的失败运行（run#1 winner=-1，旧 forced-tool 大上下文产空）给出友好空态提示「重新运行」。实测后端 `/predict/runs/{id}` 候选齐全（run3=3/run2=4/run1=0），typecheck 过、/predict 200。全弧页本就有查看按钮、未改。

### 守护模式巡检（已知极小限制，刻意不改）
- **同名跨类型实体未合并**（守护期数据巡检发现）：天之炽存在 欧米茄(item)/欧米茄(concept)、秘银(item)/秘银(concept) 两对同名实体——其实是同一事物的"实体形态/抽象概念"两种描述。去重**刻意保守、只在同 type 内合并**，因为跨类型同名常是真不同（如人物代号"欧米茄" vs 机甲"欧米茄"），强行跨类型合并会误并合法实体。影响极小（concept 副本 importance=1，重要度过滤后图谱几乎不显示），且无孤儿外键（relationships 77 边 0 孤儿、entity_states FK 0 孤儿）。结论：保持现状，不冒误并风险动工作代码。

### 本轮闭环复验（改架构后真实跑 + 逐字裁决）
- **双语 ch158（天之炽，修复后 job 4）**：中英**同一个故事**——都是黄昏光线透过翡冷翠圣母堂(Santa Maria del Fiore)彩窗、西泽尔(Cesare Borgia)独坐末排、银像凸透镜左眼把针尖光点钉在脸上、口袋纸条。英文是地道母语文学腔("a leaf of gilt that thinned to nothing before it reached the flagstones")，是中稿的**再创作**而非逐字翻译；专名还原正确(Florence/Borgia/Constantinople/Seraph)；无沙漠/Marl Voss 跑题。两次 merge 都真正融合(final_zh 3096≠zh_orig 2528，没走兜底)。已存 `天之炽_续写_ch158_中英对照_verified.txt`。
- **重写文笔 ch30（registry 改动后回归验证 job 2）**：骨架(doubao-code)干净——title/setting/pov/19 拍/8 关键信息/钩子齐全；重写(minimax-m3)5433 字网文腔，人物正确(贝隆/庞加莱/世界之蟒号)。**注册进 AGENT_REGISTRY + 钉 doubao-code 后零回归**。
- **仿写成稿 ch159（draft 主管线回归验证）**：mimic 开，`draft.writer`(minimax-m3)+三审+门控，**一稿 approve**(attempts=1，无误触返工，确定性门控生效)，3838 字，设定正确(五具炽天使/海格力斯之架/红龙/六翼猫头鹰徽记)，作者文风到位。本轮所有改动(火山 registry、en_writer 改道、translate 预算、双语改架构、predict 守卫、前端封装)对 draft/predict/re-voice **零回归**。
- **predict→arc→outline→draft 全链路（JSON-in-text 大修后端到端复验，天之炽 89k 上下文）**：predict 3 候选（文风感知，winner_reason 引用叙事基因）→ arc 2 弧各 5 真相/5 阶段（winner 兜底）→ outline 从弧产出 **11 章逐章大纲**（含 title/must_include）→ draft 一稿 approve。链路全绿。跨书：末法王座（无画像长网文）predict 3 候选通过。

## 六、续写模式预设（已落地）
文笔风格页加「续写模式预设」一键组合（非硬编码多管线，只是 mimic/bilingual 两开关的命名捆绑）：
- **长篇网文**：mimic 关 + 双语关。
- **传统仿写**：mimic 开 + 双语关。
- **西方双语**：mimic 开 + 双语开。
当前激活态高亮；仅在已有文风画像时显示（无画像的书 mimic 本就默认关，无需切换）。验证：三种组合经 `PUT /style/toggle` 均正确持久化。

## 七、双语任务阶段进度可视化（#38，已落地）
双语整章耗时长（minimax-m3 的 32000-token 融合慢，整章 ~5-15 分钟），原来前端只有一个笼统转圈。新增 `BilingualDraft.stage` 字段（`_ensure_column` 增量迁移，启动/切书自动应用），`bilingual_write(on_stage=...)` 在每个阶段边界回调，`run_and_store` 写库；GET/list 都返回 stage。前端：文笔页 `BilingualStageBar` 步骤条（中文稿 → 英文再创作 → 交叉互译 → 取长补短融合，当前步⏳、已完成✓）；章节工作台按钮实时显示当前阶段名。轮询改为每 tick 都 `setBiJob`（live 刷新 stage），完成渲染仍按 status 门控。stage 写库 best-effort（包 try/except，绝不因进度展示拖垮 job）。

### 抽取输出上限可配 + 超长章批次策略（本轮，换书《荆棘王座》《光明皇帝》暴露）
- **20. 大批次 + 固定 8000 输出 token → 长章漏抽**：荆棘王座 8 章但**单章 ~2.4 万字**，用默认「一键抽取」50 章/批（实际 8 章挤 1 批）跑完，伏笔仅 6、情节 9——严重欠采。根因与 #13/#14 同源：**瓶颈是输出 token 不是输入**，一批塞太多内容、8000 输出列不全。修复：① 抽取 `_agent_call` 的 max_tokens 从写死 8000 改为读 **`extract_max_tokens` 设置项**（`settings.store` 加 getter + `update_settings` 处理 + `get_settings` 回传；UpdatePayload 加字段；设置页「默认模型」卡片下加数字框 2000–32000）；② 策略：**按"章长 × 批大小"控制每批内容量**——荆棘王座改 1 章/批 + 20k 输出后，伏笔 6→33、情节 9→52、状态 13→52（**4–6 倍**）；光明皇帝（25 章、中位 9.5k、最长 40k）用 1 章/批 + 16k，实体 248/伏笔 84/情节 132。**通则：超长章(>1.5 万字)用 1 章/批 + 16k–20k 输出;普通章可 4 章/批 @ 8k。**（**已知残留**：光明皇帝那个 40k 终章即便单独成批仍相对欠采 ~1/3，待"章内分窗"增强。）
- **21. 全弧"一次性产 N 个候选"在长章+丰富记忆下崩成碎卡**（用户报障：光明皇帝预测全弧出 7 张大半为空的卡）：`stage_a` 让模型一次输出 N 个含深层嵌套（title/真相/阶段/势力/因果节点）的大 JSON。在重上下文（arc 上下文 ~90k chars，全是结构化记忆：248 实体/伏笔/谜团/剧情摘要 + 最近 5 章**标题** + 叙事基因，**不含原文正文**；原文只在抽取阶段读）下模型维持不住嵌套结构 → JSON 嵌套错乱 → `json_repair` 抢救成可解析但把**单个候选的子字段打散成多个数组元素**（解析出 7 个"候选"实为 ~2 个真候选的碎片，各缺 title/阶段）。**与 #14 同家族（大上下文结构化输出脆弱），但表现是"碎片化"而非"空"。** 修复（用户称"挺危险"，故强调隔离性）：`stage_a` 从"一次产 N 个"改为**逐候选循环**——每次只产 1 个自包含小 JSON（max_tokens 12k 足够单个），把已生成候选标题回灌、要求新候选方向迥异；`_valid_arc`（须有 title + ≥1 阶段）**过滤碎片、当场丢弃**；坏候选不连累其他，并补少量重试凑够 N 个。**影响面隔离**：只动 `stage_a`，不碰单章预测/大纲/成稿/抽取/记忆——爆炸半径仅"预测全弧"。实测（光明皇帝 after=25, 3 候选）：产出 2 个**完整干净**候选（#0 4阶段/6真相、#1 5阶段/5真相，winner 兜底有效），第 3 个因模型偶吐重复键坏 JSON 被过滤（故补了重试）。**结论：旧版在长章/重记忆书上必崩成碎卡→本质不可用;新版每候选独立、坏的单独丢、可补齐,是从不可用→可用的改善,非退化。**
  - **21b. 保留双策略 + 前端开关（应用户要求"原来的策略也要保留"）**：`stage_a(..., per_candidate=True)` 默认逐候选；`per_candidate=False` 走恢复的 `_stage_a_oneshot`（一次产 N 个，省一轮调用，短章/小记忆更快）。两种模式**都套用 `_valid_arc` 过滤碎片**（即"1+3"中的过滤对两模式通用）。参数链：`/arc` 页单选「逐个生成 / 一起生成」(默认逐个)→ `api.arcRun(...,perCandidate)` → `ArcRunRequest.per_candidate` → `run_arc` → `stage_a`。UI 旁附提示：长章/记忆大用逐个(稳)、短章/记忆小用一起(快)。
  - **21c. 根因辨析:不是"章节太长"单因,病灶在输出端。** 用户追问"一起生成 JSON 出错是不是因为光明皇帝章节太长"。准确答:**病灶 = 让模型一次性产出"N 个深层嵌套大对象"**(每候选套 `title/核心真相[]/阶段[]/势力[]/因果节点[]` 多层),长输出里嵌套配对(括号/引号/逗号层级)本就脆弱,越往后越易错位;一旦错位 `json_repair` 抢救成可解析、却把单候选子字段打散成多个数组元素 → 解析出"7 张卡"实为 ~2 个真候选的碎片(缺 title/阶段),即"大半为空"。**章节长是放大因素而非病灶**:注意全弧预测**不读原文正文**——`_gather_context` 的 `select(Chapter)...limit(5)` 只取最近 5 章**标题**,上下文(`_ctx_blocks`,~90k chars)全是抽取阶段沉淀的结构化记忆(伏笔/谜团/世界规则/人物状态/剧情摘要/叙事基因)。长章的影响在**抽取阶段**:章越长→抽出的实体/伏笔/剧情点越多(光明皇帝 248 实体)→arc 记忆上下文越重→模型维持结构余量越少→同样"一次产 N 个"越易崩。故短章书(记忆少)一起生成大概率不崩、光明皇帝这种长章书(记忆多)几乎必崩。(更正:本记录早先误写"5 章采样 ~4.7 万字原文"——那是抽取阶段读的原文字数,非 arc 预测上下文;arc 只读记忆。)**与 #14(大上下文强制工具吞输出)同家族**——大上下文结构化输出脆弱,这次表现为"碎片化"而非"空"。真正杠杆在输出端:逐候选=每次只产 1 个小 JSON(输出小/嵌套浅→稳),长章不再致命。
  - **21d. 实证复盘 + 更正 21c 的过度二分(用户追问"末法王座体量更大为何当时没崩")。** 翻 DB 实测两本书全弧历史,推翻了"短章必不崩/长章必崩"的简化结论：
    - **末法王座当时其实也崩过**：8 次 arc 中 run4、run5 `candidates_json` = `[]`(空,len=2),run3/6/7/8/9/10 完整。用户记忆"没崩"是因为崩后重试出了完整结果。
    - **两次崩形态不同(因中间做了 #39)**：末法(2026-05,arc 还是 forced tool_choice)崩=**空输出**;光明(2026-06,#39 改 JSON-in-text 后)崩=**碎片化**(run1 出 7 张卡仅 1 张有 title)。同病(大上下文结构化输出不稳)、改造前后两种表现。
    - **"体量更大"是错觉**：arc 上下文不含原文、与总章数(1472 vs 25)无关,只看记忆密度,两本量级接近——末法【重要人物 87 + 世界规则 208】更重,光明【伏笔 27】更密;不存在"末法大得多"。
    - **正确结论 = 概率性失败**,非确定律:末法同参数 run4/5 崩、run3/6/7 成;光明 run1 崩、run2 成。一起生成在大上下文 + 一次性深嵌套大输出下不稳,撞上就崩。逐候选的价值是把单次输出做小做浅→崩概率压低 + 坏卡单独丢,**不是"长章专属修复"**。故 21c 中"短章大概率不崩、长章几乎必崩"应修正为上述概率性表述。
    - **附带发现**：光明皇帝 person importance≥5 = 0 → arc characters 块为空,看不到人物状态;系抽取未给人物打 importance 的独立质量问题,待修。

- **22. 抽取后 importance 不重算 → arc/图谱人物块空(承 21d 附带发现)。** 根因:`Entity.importance` 抽取期是 `+1` 提及计数(注释"computed from mention count"),**跨书不可比**——长书累加上千、短书/少批次停在 1~3;`backfill_importance()`(`1+5*plot+3*state+2*fs`,纯 DB 重算无 LLM)只在 `bookwriter` 阶段边界和手动 API `/graph/recompute-importance` 调,**`run_all`/`run_batch` 抽取链路从不自动调**。只抽取不写整本的书(光明皇帝/荆棘王座)importance 全停在 1~3,arc 的 `importance>=5` 人物块 → 0 人。验证:光明皇帝 plot 关联实体 131/132、伏笔 81/84、state 108 行——**信号本就齐全**,只差没重算;手动跑一次 backfill → importance≥5 人物 0→51(叶羽 450/谢童 250/魏枯雪 207…),`_gather_context(25)` characters 块也从空→51 人。修复:`extract.py` 加 `_safe_backfill_importance()`(try/except 包裹,失败不连累抽取);`run_batch(finalize=True)` 单批/重试路径收尾重算;`run_all` 给每批传 `finalize=False`、所有批跑完后**主线程统一重算一次**(避免 workers>1 并行时多线程全表更新 importance 写竞争)。已对存量 5 本书补跑 backfill:末法 林云 1077、荆棘 西泽尔 155(原最高仅 2)、龙族 路明非 322、天之炽 西泽尔 2897、光明 叶羽 450,主角全部归位。(DB 在 data/ 已 gitignore,属可重建缓存,改的是代码使其今后自动重算。)

- **23. 逐章大纲「骨架→填充」逐步生成(与一次性并存,开关切换)。** 动机:`outline.refine` 单 phase 一次性产 5–15 章,12k token 被均摊→长 phase 细节浅、且后章看不到已定前章→易矛盾/重复。新增 stepwise 模式:遍1 一次产整段轻量骨架(index/title/intent/beat + 认领 key_event/伏笔,`outline.skeleton`,max 6k),遍2 逐章展开完整字段(`outline.flesh`,max 4k,喂入骨架+前序章节钩子+待覆盖 key_event 预算管家)。`pipeline.refine(mode="oneshot"|"stepwise")` 分发,产物同形(下游 persist/project/draft 零改);骨架空时自动降级回 oneshot,单章 flesh 失败用骨架兜底不留空洞。前端 /outline 页 + arc 卡片各加开关。**踩坑**:骨架/flesh 走 JSON-in-text,模型把 JSON 包在 ```json 围栏里,而解析器没剥围栏→首测骨架判空降级;修 `_strip_fences` 后正常(沿用 predict `_loads_json` 的剥栏做法)。
  - **23b. 实测结论(诚实记录:本次对比 stepwise 未胜,反而略差)。** 光明皇帝 ArcRun2 winner phase3 截 5 章(63–67),两模式各跑一次:oneshot must_include 均 3.6/章、字符密度 183、5 章各司其职、伏笔回收分散具体;stepwise must_include 均 2.8、密度 144、beat 扎堆"泰山/重铸"、ending_hook 连续三章公式化("大道转身…该开始了")。**即 stepwise 在此样本更套路、更薄。** 关键辨析:**测试落在错误区间**——5 章时 oneshot 的 12k 额度根本没被摊薄(2400 token/章,绰绰有余),而 stepwise 的预设收益(治"额度被摊薄")只有在 **10–15 章长 phase** 才显现;5 章反被"骨架强约束 + 公式化承接"拖累。结论:stepwise 不是普遍更优,**应只在长 phase 用**(与开关提示一致),且骨架质量是天花板(本次骨架把重铸 beat 扎堆);短 phase 用 oneshot 更好。待办:在 10–15 章长 phase 上复测(耗火山额度,需用户授权),并考虑强化 flesh 提示(逼更厚 must_include、禁套路化 hook)。
  - **23c. 长 phase(13 章)复测结论:stepwise 在目标区间依然完败(假设被推翻)。** 光明皇帝 ArcRun2 winner phase0(26–38,完整 13 章):oneshot must_include≈3.5/章、密度 180–193、ending_hook 13/13、must_avoid 13/13、~100s/1 调用;stepwise must_include 2.2、密度 129、ending_hook 仅 8/13、must_avoid 8/13、732s/~15 调用/$0.0336。**真正原因:"12k 额度被摊薄"前提不成立**——大纲是结构化 JSON 非散文,13 章一次产每章仍有 ~920 token,够用;故 oneshot 长 phase 照样写得密。骨架预先把每章钉成薄 beat→填充只能照做、丢了 oneshot 的全局伏笔编织;填充模型更省字(常只 2 条 must_include、漏 hook)。复测期间修了 2 个 stepwise 健壮性 bug(骨架偶发判空→重试;列表字段含 dict→`_str_items`)。**最终采用:oneshot 维持默认(短/长 phase 都更优);stepwise 保留在开关后但不推荐常规用;翻盘方向是调填充提示而非现状。** 详见《墨笔-大纲骨架填充对比测试.md》。

- **24. 全弧"推荐续写章数"启发式建议(前端显示)。** 用户:目标延展章节不该让用户凭空填,应根据 现有体量 + 未填的坑 + 叙事节奏 给个推荐值显示在前端。实现:`recommend_target_chapters(after_chapter)`(`predict/pipeline.py`,纯启发式、零 LLM、可解释)——谜团按 severity 给基准篇幅(core 18 / major 9 / minor 3 章,各一条要相应篇幅来收),open 伏笔超出"谜团可顺带覆盖部分(每谜团带 2 条)"的按每条 +1 章;再乘 体量乘子(中位字数 vs 3000,clamp 0.6–1.4,长章书每章装更多→更少章)与 节奏乘子(文风画像 pacing:快 0.85 / 慢史诗 1.2 / 适中 1.0);最终 clamp 15–500,给推荐值 + ±20% 区间 + 中文 rationale。端点 `GET /predict/arc/recommend-chapters?after_chapter=`;前端 /arc 页随"起始章节"防抖 400ms 拉取,在"目标延展章节"旁显示「💡 建议 ~R 章(L–H)」+「采用」按钮(一键 setTarget)+ 依据说明。实测光明皇帝 after=25 → 56 章(2 核心+5 重要谜团、27 伏笔、中位 9511 字/章压密度乘子至 0.6)。

- **25. 厘清「故事弧预测 / 整本推演 / /outline / /draft」的分工(用户多次困惑:粒度都说"逐章",是否冲突重复)。** 结论:**不冲突、不重复,是同一条流水线的不同环节 + 同一引擎的两个入口**。
  - **链路**:`故事弧预测(stage_a)` → `逐章大纲(OutlineRun)` → `成稿(/draft)`。
  - **粒度分层**:预测产出的是**阶段级宏观蓝图**(每个候选含 core_truths/phases/结局/自估总章数,是"到结局的整本剩余故事",但只到 phase 概要);逐章大纲才是**章级施工图**。
  - **「整本推演」vs「/outline 页」= 同一引擎 `outline.refine` 的两个入口,写同一张 `OutlineRun` 表**(架构 #48 RECONCILE 单一数据源):
    - 整本推演(`project_full_book`)= **批量编排器**:把选中候选的所有 phase 一键逐阶段展开成连续逐章大纲 + 范围重锚定 + 完整性裁决,落成多条 OutlineRun。
    - /outline 页 = **单阶段手动生成 + 大纲工作台**:查看/微调推演批量产出的逐章大纲(也能单阶段补做);`/draft` 照此成稿。
  - **真实的"数字不一致"隐患(非 bug,是 UX)**:预测时填的「目标延展章节」(软提示)、候选自报的 `total_chapters_estimated`、新加的「建议章数」三者独立;**`project_full_book` 推演时只用候选自己的 phases + total_chapters_estimated,完全不读「目标延展章节」**——即你设 100、候选出 65,推演按 65 走。已在首页 workflow 的 ② 组 note 与各步描述里点明该链路与同表关系,降低困惑。
  - **25b. 实跑验证(光明皇帝 ArcRun3 winner#0「光明劫后」4 阶段)**:① 技术上跑通不报错,4 阶段范围**干净重锚**(26–35/36–50/51–60/61–65,与候选原范围一致、无缝隙重叠);② 完整性裁决=**needs_work**,自动揪出真实问题——1 条核心问题未交代(光明海剑/清净光铠出处)、3 条伏笔未收(30/83/4)、**时间线错乱与设定矛盾**(ch42-43 与 ch37-41 顺序突兀;ch46 已揭"十二宝树王",ch41/44 又说"十一宝树王"自相矛盾)。**这正说明裁决环节有价值:它把推演内部的剧情冲突显式暴露,而非默默通过。** ③ 一个量级现象:候选阶段范围声明到第 65 章(40 章),实际逐章只产出 31 章(26–56)——outline 每阶段 oneshot 产出比声明范围**偏短**,故推演出的全书会比候选自估短,使用时需知晓(后续可在 refine 里按范围补足章数)。

- **26. 因果图节点全堆叠成一列(用户报障:标题 12 节点但只见 5 个框重叠)。** 根因:`CausalFlowGraph` 的 `NODE_SIZE` 用 `{w,h}` 键,而 dagre 布局要的是 `{width,height}`;`g.setNode(n.id, {w,h})` 传错键 → dagre 把每个节点尺寸当 **0** → 仅靠 ranksep/nodesep 间隔、节点无footprint → 12 个 200px 宽的框 x 坐标全挤在 0–100 区间彼此重叠(实测错误键 x∈[0,100],正确键 x∈[100,600] 正常分叉)。数据本身没问题(n1–n12 id 唯一、13 条边的正常 DAG)。修复:`g.setNode(n.id, {width: sz.w, height: sz.h})` 一行。纯前端、零风险。

- **27. 「目标延展章节」语义 + 推演范围(用户追问:设 2 章也到结局?推演会超出 arc 补全到结局?)。**
  - **Q1 — 「目标延展章节」是预测到结局吗?** 是。**故事弧候选永远规划到大结局**——schema 强制每个候选含 `core_truths`/`climax_synopsis`(高潮)/`ending`(结局)。「目标延展章节」**不是"写到第几章为止"的终点开关,而是"用大约多少章把整本剩余故事讲完"的节奏/篇幅软提示**:设 100→铺成约 100 章的慢节奏;设 2→仍规划到结局但把全部真相+高潮+结局硬压进 2 章(节奏崩、不可用)。两点补充:① 软提示,模型常按自己的 `total_chapters_estimated` 来(你设 100 它可能出 65);② 前端输入框 min=20,实际填不进 2。
  - **Q2 — 整本推演会超出 arc 范围、补全到结局吗?** 不会"超出",因为不需要——**选中候选本身就已规划到结局**。`project_full_book` 严格按候选自己的 phases 逐段展开,最后一个 phase 就是终局;推演只是把蓝图**细化**成逐章,不延长、不改写终点。实测光明皇帝 #0 候选(4 阶段到第 65 章):推演到候选结局为止,唯一偏差是产出**偏短**(声明 65 章、实产 31 章到第 56 章),是"没铺满"而非"超出补全"。
  - **一句话**:arc 候选 = 永远含结局的完整蓝图;「目标延展章节」只调用多少章讲完(节奏);推演 = 忠实地把蓝图(含结局)细化成逐章,不超范围、不另造结局。已在 /arc 页标题与「目标延展章节」label(tooltip)标注"预测整本到结局、此处仅篇幅提示"。

- **28. 《九州·缥缈录》语料切分失败「no chapters detected」(前端显示 TypeError: Failed to fetch)。** 根因:章节正则 `^(?P<title>第…章…)` 顶格锚定行首,而九州标题带全角空格缩进「`　　第一章 蛮荒〔一〕`」→ 第 不在行首 → 零匹配 → `ingest` 抛 RuntimeError→HTTP 500;前端 `j()` 把 500(可能缺 CORS 头)呈现成 "Failed to fetch"。修复:正则放开前导空白 `^[ 　\t]*(?P<title>…)`(半角/全角 U+3000/Tab),并把正文偏移改用 `m.start("title")`(前导空白不计入,切片干净)。实测九州切出 **237 章**(多卷重启"第一章",含〔一〕〔二〕分段,按阅读顺序重编号)。其它书顶格标题不受影响。(附:前端把后端 500 一律显示成 "Failed to fetch" 是独立 UX 小瑕疵——500 响应可能缺 CORS 头,待后续给错误响应补 CORS / 在 j() 里区分网络错误与 HTTP 错误。)

- **29. 一键抽取「每批章数/并发」默认值按体量自动推荐(用户:不该固定 10/4)。** 抽取是输出 token 瓶颈(#20),每批文本越多越易漏抽,故每批章数应与每章字数成反比。新增 `GET /ingest/recommend-batch`:按已切分章节的**中位字数**算 `batch_size = clamp(round(22000/median), 1, 12)`(目标每批 ~22000 字舒适窗口)、`workers = 2(长章>12k字)/3(中)/4(短章<4k字)`,带 rationale 与预计批数。前端 ingest 页**每本书自动带入一次**(用 `appliedBook` ref 记 slug,用户改过不覆盖),显示推荐与「采用推荐」按钮;并放开每批输入最小值 5→1(长章需 1 章/批)。实测:九州(中位 3848 字/237 章)→ 每批 6、并发 4、约 40 批;长章书(如荆棘 2.4 万字)→ 每批 1。

- **30. 失败批次「重试」静默崩溃(唯一键冲突)——重试根本没跑(用户报障:点了重试无反应)。** 现象:`POST /ingest/batches/{id}/retry` 返回 200,但后台 `run_batch` 抛 `IntegrityError: UNIQUE constraint failed: extraction_batches.chapter_start, chapter_end`——因为 `run_batch` 每次都**新建**批次行,而那条 failed 批次的同范围 `(start,end)` 还占着唯一键。错误只进后端日志、不回传前端,故用户只见"重试无效"。**对 `run_all` 重跑同样成立**(其 overlap 检查只跳过 done/running/pending,不含 failed → 重排该范围 → 新建撞键)。修复:`run_batch` 建行前**先删除同范围的旧批次行**(重跑即取代旧记录),一处修复覆盖 retry 与 run_all 两条路径。实测九州补跑:旧 failed#34 → 新 #41 done、无 IntegrityError、批次 37done/3failed → 逐步补齐;重试路径已通(`finalize=True` 自动 backfill importance,承 #22)。另:这些批次原失败是 429 AccountRateLimitExceeded(provider 限频,workers=4 并发突刺所致),单批串行重试时限频已缓解→成功。

- **31. 文笔风格页渲染崩溃「Objects are not valid as a React child」(九州 20 章画像)。** 根因:模型把本应是字符串的 `structural_habits` 返回成对象 `{chapter_title, chapter_ending, chapter_structure}`,而 `/style` 页 `<Field v={p.structural_habits}>` 直接当 React child 渲染对象 → 抛错。修复:新增 `renderVal(v)` 递归渲染器(字符串/数字直出、数组→`<ul>`、对象→键值网格),`Field` 改用它,并把直接渲染的 `overall_voice`/`scene_styles` 值/`continuation_guide` 也包上——任何字段被模型返回成对象/数组都能可读展示而非崩溃。纯前端容错,不依赖模型严格守 schema。
  - **31b. 同页两个衍生渲染问题(20 章画像 markdown + 长串溢出)**:① 模型在 tropes/vocab 字段里夹了 markdown `**`→字面显示;② signature_vocabulary 被塞成一条超长「A、B、C…」字符串,grid 子项缺 `minWidth:0`→长串撑破轨道、把邻列"常用套路"挤成一字一行。修复:`cleanMd()` 去 `**`/行首列表符(renderVal 字符串统一过滤);tropes+vocab 区 grid 改 `auto-fit minmax(280px,1fr)` + 子卡 `minWidth:0` + `overflowWrap:anywhere`;词汇按顿号/逗号拆成多个标签、标签可换行。

## 八、待办 / 延后
- 真·倒序/环形书的逐章故事时间线元数据（延后；天之炽线性+插叙够用）。
- 续写模式预设可进一步做成「书架级」默认（每本书记住偏好），当前为文笔页即时切换。
