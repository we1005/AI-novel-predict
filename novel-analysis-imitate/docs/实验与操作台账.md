# 实验与操作台账(运行日志)

> **规矩**:本仓库之后的每一步**操作 / 实验 / 测试 / 修复**都按下面格式追加一条,**先做、后即时沉淀**。
> 目的:任何结论都可回溯到"做了什么命令、得到什么数、跑在哪、commit 是谁",杜绝"凭印象"。
>
> **条目模板**(复制后填写):
> ```
> ## YYYY-MM-DD · <一句话标题>
> - 动机:为什么做
> - 操作/命令:`...`(可复跑)
> - 结果:关键数字 / 表 / 产出文件
> - 结论:从数据得出什么(含反例与不确定性)
> - 落地:改了哪些文件 + commit 短码;遗留/下一步
> ```
> 详细分析另开专题 md(如 `agentic-search-消融结论.md`),本台账只做**时间线索引 + 关键数 + commit**。

---

## 2026-06-28 · agentic style-search:是否过度设计(三轮消融 + 重合度 + 公平复赛)

专题详档:[`agentic-search-消融结论.md`](agentic-search-消融结论.md)。脚本:[`experiments/`](experiments/)。

### ① 检索基座 + 自我污染修复
- 动机:用户提出"写作时 LLM 调 search tool 检索素材库"。先建可对比地基,不直接重构。
- 操作:新增 `backend/app/craft/search.py`(`search_corpus` 章节正文 BM25 + `search_snippets` 笔法库);
  修 FTS5 trigram 必须 ≥3 字(2 字概念词零召回)→ 滑动 3-gram + OR。`draft` 风味召回排除已生成章(防自污染)。
- 结果:对余烬之铳验证每主题 3 条相关命中;回归 8/8。
- 落地:`f99965e`(基座+脚手架)、`a423010`(自我污染)。

### ② 三组消融 v1(无参考 / push / agentic)
- 操作:`backend/.venv/bin/python novel-analysis-imitate/docs/experiments/ablation_search.py`
- 结果(余烬之铳,4主题×2样本):fidelity A/B/C = 32.0/30.5/31.6(分不开);**盲评 = 37.9 / 66.7 / 45**。
- 结论:push 检索参考把盲评 +29(有用,但只在 holistic 盲评显现);agentic(45)反不如 push(此结论后被 ② 的 bug 推翻)。
- 落地:`df834b2`(结论 + `ablation_search_results.json`)。

### ③ 重合度校验(push 高分是真迁移还是照抄?)
- 操作:`...ablation_overlap.py`(LCS + ≥8字片段覆盖率,plain 作偶然基线)
- 结果:push 平均 LCS 6.1 / 覆盖 1.2%;plain 2.9 / 0%。
- 结论:**push 高分不是照抄,是真风格迁移**(最严重一例抄 18 字,整体可忽略)。保留:本轮注入为短 FTS 片段。
- 落地:`dc84292`(+ `ablation_overlap_results.json` 含样本产出)。

### ④ craft 笔法库填库
- 动机:圆"按景物/建筑标签检索"的设想——之前 craft_snippet 三书皆空。
- 操作:`pipeline.extract_all(batch_size=5, max_chapters=120, slug="余烬之铳")`
- 结果:**439 片段 / 26 类 / 26 风格卡**(scene_env 28、scene_place 12、object 14、combat 35…);库在 gitignored novel.db。
- 结论:`search_snippets(category="scene_place")` 即"按建筑景物检索",数据就绪。

### ⑤ agentic 公平复赛 v2(整段注入 + 自选类目 + 重合度)+ 自查 token bug
- 操作:`...ablation_search_v2.py`(注入 craft 整段 excerpt;C agentic 自选类目+查询)
- **闭环自查**:首跑 agentic 总退化成 `['scene_env']`+主题原句 → 单独验证规划步发现根因是规划调用
  `max_completion_tokens=400` 截断 mimo → JSON 解析失败 → 退化默认值(同 E5/基因组 token 坑)。修到 1500 重跑。
- 结果(token 修复后):盲评 A/B/C = 46.7 / 50.6 / **50.0** → **agentic 与 push 打平**(非"输");重合度≈0(无抄袭);
  类目级检索 +4 远逊 v1 关键词级 +29。
- 结论:① 检索参考有用但**收益取决于精度**(关键词 >> 类目);② **agentic ≈ push**,多一次规划调用换同等质量 →
  **不做默认,留 opt-in**(多主题/跨书场景);③ 真正杠杆是检索精度,非 push/pull。
- 落地:`4be1b27`(v2脚手架)、`88de2e0`(修 token 截断)、`5c8e98a`(v2 结论 + 结果)。

### ⑥ CI 修复(顺带)
- 现象:GitHub Actions `tests` 全红。
- 自查:全新 venv 只装 `requirements-test.txt` 复现 → `app/config.py` 依赖 `python-dotenv` 未列入。
- 落地:补 `python-dotenv` 后干净环境 8/8;`f4dbeb2`。

### 本专题定稿决定(保留双轨)
- **PUSH 默认**(同质量更省一次调用);**检索优先走具体话题关键词**(精度杠杆)。
- **agentic PULL 留 opt-in**(基座 `craft/search.py`/`search_snippets` 就绪;打平非输,给多主题/跨书场景)。
- **标签库值得填**(craft 抽取已验证可用)。
- 待办:把"具体话题关键词检索"接进真实 writer(push 增强,纯增量、可 A/B);agentic opt-in 接写作 flag 真上端到端。

## 2026-06-28 · 红蓝对抗 E 系列待修收尾(E8/E3/E6/E4)

接第三轮 E1–E8 汇总表,把剩余「待修/部分修」逐条做完。全部带确定性单测,回归从 11 → 16/16。

### E8 · EntityState 状态/diff 对账(gained∩lost)
- 病:同章既「获得」又「失去」某物时,state 与 diff_json 不自洽(双记/漏减)。
- 修:`extract.py` 加模块级纯函数 `reconcile_items(prev, gained, lost)` → 净出 gained∩lost,
  state 与 net_gained/items_lost 一致可逆;`_persist_states` 落归一化 `applied` diff + `_reconciled` 标记。
- 测:3 例(纯得 / 同章得失抵消 / 失已有)。落地见对应 commit。

### E3 · 重切章孤儿行 + schema 漂移
- 病:重切章后旧章号派生行变孤儿(指向已不存在/含义已变的章),静默累积 + 双计。
- 修:`split.py` 重建 chapters/FTS 后,按「chapter > 新max」原始 SQL 清理 11 张派生表孤儿
  (try/except 逐表,缺表跳过)+ 伏笔回收点越界置 NULL;返回 `orphans_pruned`。
- 注:完整 Alembic 迁移框架列为更大专项,暂缓。

### E6 · 429 成本放大 + 跨 provider 故障切换(默认关闭)
- 病:`@retry` 限流重试 7×(15–120s)→ 成本放大 + 单 provider 宕机整链 hang ~14min,无降级。
- 修:`settings` 加 `get_fallback_model()`(读 settings.json `fallback_model`,默认空=不启用);
  `call` 拆 `_call_impl`(@retry 原体)+ `call` wrapper:主模型失败后若配了 fallback(应属另一 provider)
  降级再试一次,否则原样 re-raise;`_adaptive_stop` 配降级时限流只重试 2 次快速短路。
- 默认零行为变化(未配 fallback 完全等同历史)。测:2 例(未配 re-raise / 配了降级)。落地 `(本次)`。
- 残留:未配 fallback 时 ×7 重试为限流固有(已文档);要用降级须在 settings.json 配另一 provider 模型 + 填其 key。

### E4 · 仿真每轮活状态 + 产物回灌(此前仅修了 profile 边界 >=200)
- 病1 每轮快照:`initial_state` 全程冻结,每轮 `my_current_state` 同一份 → 角色拿不到自身上轮结构化状态。
- 修1:维护 `live_state`,每轮 `_fold_round_into_state` 折入各角色 `events_during_sim` 轨迹,下一轮带演进;
  `round_record` 增 `state_snapshot`(模型注释早预留)。
- 病2 回灌:`final_text` 从不落 ChapterDraft,`SimulationRun.chapter_draft_id` 有列never写,docstring 第6步是空话。
- 修2:落 `status="draft"` 的 ChapterDraft 并回填 `chapter_draft_id`,使产物进*既有的、有人审 gate* 的复审/回灌链
  (草稿被接受才 `extract_one_chapter` 回灌 canonical EntityState)。
- **关键设计判断**:**刻意不**在仿真里静默回灌 canonical EntityState——仿真是假设性的,
  直接写回真值会用推测内容污染记忆,比原 bug 更糟;正确通路是 draft → 人审 → 接受。
- 测:3 例(轨迹累积 / 非dict slot 稳健 / 标题抽取)。回归 16/16。落地 `(本次)`。

### 仍开放(非本批,记录在案)
- ~~E2:向量层「启用 vs 删除」待用户定夺。~~ → 已定:**启用**(见下节)。
- E3 完整 schema 迁移框架(Alembic)——更大专项。
- TODO #78/#79:话题关键词检索接进 writer(push 增强)/ agentic opt-in 写作 flag 端到端。

## 2026-06-28 · E2 定夺=启用:语义检索向量层(前端开关 + 手动加载)

- 背景:向量层(chroma + bge-large-zh 语义检索)此前是"半接死代码"——`index_chapters` 零调用、
  依赖没装、`/recall` 向量路无人消费。用户拍板**启用**,并定设计:**前端一个开关,默认启动不加载模型,手动加载**。
- 关键认识:`memory/vector._embedder` 本就 `@lru_cache` 惰性——后端启动天然不加载模型,开关只控制
  "是否走向量路 + 是否允许手动建索引"。
- 实现:
  - settings 增 `vector_recall_enabled`(默认 False);顺手修 `_load_raw` 重载丢标量额外项的潜在 bug
    (`extract_max_tokens`/`fallback_model`/本开关 现都跨重启留存)。
  - `memory/vector` 增 `deps_available`(find_spec 只探测不导入)/`model_loaded`/`indexed_count`(缺依赖返 0 不抛)/
    `reindex_active_book`(从活动书拉章节+语料全量重嵌)+ 进度跟踪 + `_reset_collection`。
  - `memory/api`:`/recall` 按开关启用(关→不碰向量、`vector_error="disabled"`);新增
    `GET /memory/vector/status` + `POST /memory/vector/reindex`(后台 + `book_scope` 绑定 + 未启用/缺依赖拒绝)。
  - `draft._gather_style_refs`:开关开时增向量语义补充段(召回"意思近但用词不同"的原著片段,排除已生成章)。
  - 前端设置页加「语义检索(向量层)」卡:开关(即时存)+ 依赖/模型/已索引状态 + "加载模型并建立索引"按钮(3s 轮询进度)。
  - 依赖 `chromadb`/`sentence-transformers`(pyproject 早声明)装入 venv。
- 验证:回归 **18/18**(+2 E2);`docs/experiments/vector_smoke.py` 用假嵌入+临时 chroma 证明
  **index/query/before_chapter 过滤/reset 全链路通**;前端 `tsc` 0 错。
- 残留(用户机器上最后一公里):首次点"建立索引"会从 HuggingFace 下 bge-large-zh(~1.3GB);本沙箱 HF 不在网络白名单,
  故真实嵌入未在此跑,但管路已证。
- 落地:`(本次 feat(E2) commit)`。

## 2026-06-28 · #78 话题关键词检索 push 增强(默认开 + 一键 A/B)

- 背景:消融(本文档 ②⑤)已证「关键词级 push 检索」盲评 +29、远胜类目级 +4,且定「PUSH 默认」。
  但 writer 里只有一段"顺带版"(修自我污染时加的:must_include 前 2 词、每词 k=1、写死常开、无开关),
  既不是完整 #78,也无法 A/B。用户要:**默认开** + **一键对比运行器**。
- 实现:
  - settings 增 `topic_push_enabled`(默认 True,跨重启留存)。
  - `draft._gather_style_refs` 加 `topic_push` override(None=随全局 / True/False=强制供 A/B);
    门控话题检索(关=基线只近章正文),强化为前 3 词×k=2;每条 ref 打 `source`(recent/topic_push/vector);
    `gen_chapters` 提到公共处供 push 与向量两块共用。
  - `write_chapter` 透传 `topic_push`、返回 `topic_push_refs` 命中数。
  - 一键 A/B:`pipeline.ab_topic_push` 直接调 `_writer_call` 两遍(push 关/开),隔离单变量、不落库;
    盲评随机甲/乙序去位置偏差(`_map_ab_winner` 纯函数映射回 off/on)。端点 `POST /draft/ab-topic-push`;
    前端写作页「🔬 push 对比」按钮 + 并排弹窗;设置页开关卡;CLI `docs/experiments/ab_topic_push.py`。
- 验证:回归 **20/20**(+2:开关默认开且跨重启留存、盲评位置去偏映射);前端 `tsc` 0 错;
  后端路由含 `/draft/ab-topic-push`。**真实 A/B 胜率待用户在本机用一个真 OutlineRun 跑**
  (写整章需 live LLM,沉淀到此处)。
- 落地:`(本次 feat(#78) commit)`。

## 2026-06-28 · #78 真实 A/B 实测(天之炽 3 章 · live LLM)

- 动机:#78 落地后用户要"你帮我跑"真实胜率(此前只证了管路,未跑 live)。
- 操作:`backend/.venv/bin/python novel-analysis-imitate/docs/experiments/ab_topic_push.py
  --slug 天之炽-江南 --outline-run 10 --chapters 247 251 255`(每章写两遍:push 关基线 vs 开增强 + 盲评)。
- 结果:**push 开胜 2 / 基线胜 1 / 平 0**(成本 $0,天之炽走火山 coding-plan 订阅模型)。
  机制确认生效:push 开侧除近章 [153–156],额外召回话题相关历史章(247→82/26/126;251→41/27/61;
  255→48/136/45),每章注入 3 条 topic_push 范例。
  - ch247 push 胜:项链异象/通缉令对话/地下指挥中心 等具体质感更强,贴原著紧张细腻腔调。
  - ch251 **基线胜**(诚实反例):基线更冷峻克制、牛皮纸包/机械鸟焊痕等细节更扎实;push 侧部分对话略生硬。
  - ch255 push 胜:锡兰圣歌唤醒欧米茄原型体的"群体意识觉醒"核心场景刻画更充分连贯。
- 结论:小样本(n=3)指示性而非定论——**push 倾向带来更扎实的话题质感**(2/3 胜),但**并非每章都赢**
  (ch251 基线的冷峻克制反而更贴该书腔调)。与消融 ②⑤"关键词级检索有用、收益取决于精度"一致。
  默认开合理;个别追求极简冷峻的章可手动关。需要更硬的数字应跑 ≥10 章。
- 落地:结果全文存 `docs/experiments/ab_topic_push_天之炽_results.txt`。

## 2026-06-28 · #79 agentic 检索 opt-in 实现 + 真实 A/B(天之炽)

- 实现(一次性规划器版,用户选定):settings 增 `agentic_search_enabled`(默认**关**,opt-in,跨重启留存);
  `draft._agentic_plan`(一次 FAST 调用 JSON-in-text 让模型自选话题/类目查询)+ `_agentic_style_refs`
  (据规划查询从原著检索,标 source='agentic');`_gather_style_refs` 加 agentic/chapter_outline 参数
  (agentic 开→自取,取代 push 的 must_include 直查;否则 push;都关=基线)。`write_chapter` 透传 agentic。
  一键 A/B `ab_agentic_search`(push 臂 vs agentic 臂)+ 端点 `/draft/ab-agentic-search` + 前端「🧭 agentic 对比」
  按钮 + 结果弹窗自动识别两种模式 + 设置页 opt-in 卡。
- **过程中发现并修一个真 bug**:`_ab_judge` 改签名加 label 后,内部 `_map_ab_winner` 调用漏传 label_a/label_b
  → agentic A/B 胜出方被误标 on/off。首轮跑才暴露(单测只测 _map_ab_winner 本身,漏了 _ab_judge 转发)。
  修后加集成测试 `test_e79_ab_judge_forwards_labels`。回归 23/23。
- 真实 A/B(天之炽-江南 run#10,ch247/251,两轮):
  - 第一轮(标签 bug,按默认映射反解):247 agentic 胜、251 push 胜 → 1:1。
  - 第二轮(修复后,标签干净):**agentic 2 / push 0**。
  - 合计 4 章次:agentic 胜 3 / push 胜 1。
  - 规划器产出的查询具体可用(如「圣堂装甲师巡逻车+飞艇的压抑景象」「项链紫瞳特写」「地下斗兽场据点环境」),
    且检索到与 push 的 must_include 直查不同的章(push 247→82/26/126;agentic 247→126/78/3)。
- 结论:**agentic ≥ push(小样本占优,但 run-间方差真实**:规划器/writer/裁决均带温度,同章两轮结果会变)。
  与消融"agentic≈push、多一次规划调用"一致 → **默认关、opt-in 合理**;追求"模型按本章自适应取材"时开。
  更硬的数字需 ≥10 章多轮平均。
- 落地:结果 `docs/experiments/ab_agentic_天之炽_results.txt`;代码见本次 feat(#79)+fix commit。

## 2026-06-28 · 头脑风暴:从"逼近作者"转向"提取通用类型模板"(reframe)

- 动机:用户重新定位北极星——流水线网文不必追求对某作者 100% 文风保真;真正要的是**某一类**(克苏鲁+维多利亚+蒸汽朋克)
  的**可复用、可杂糅多作者、可灵活运用的通用模板**;"贴原作者"退化为续写场景里很细的一个可选挡位。要求先广撒网头脑风暴、不验证。
- 操作:多视角 workflow(7 棱镜并行发散:场景全景/类型模板/多作者融合/可控性/评估重构/架构落点/反直觉跨界 → 综合 → 完整性批判补漏)。
  9 agents,~355k token,~9.5 分钟。
- 产出:`docs/写作场景与通用类型模板-头脑风暴.md`(315 行,9 章 + 完整性批判)。要点:
  目标"指纹→做菜法";相似度→"对题/不塌方/够新";"最大公约数+辨识度"而非求平均;26 场景归并 7 大类;
  类型模板 9 部件 + 类型基因 vs 作者基因分层;多作者融合冲突五策;双正交滑杆(风格强度×自由度);
  评估从 fidelity→fitness-for-purpose(分场景标准);fused_* 提级为一等 `genre_template`;13 条待验证 backlog。
  批判补漏:诊断/止损反向场景、计划内漂移豁免、多语言、成本挡位、版权合规、给人看的"可读配方";
  并点破内在张力(去签名 vs 要辨识度 互斥、共性可能=最 cliché)。
- 结论:**纯发散,未验证**;下一步从第 9 节 backlog + 批判里挑高价值项排优先级再做。
- 落地:`(本次 docs commit)`。

## 2026-06-28 · V1(#2)方差当判别器(确定性,不烧 token)

- 操作:`docs/experiments/v1_fingerprint_variance.py`(6 书 × 8 确定性指纹维 → 簇间/簇内 F 比)。
- 结果:hedge_per_kchar **F=28.6**(余烬克苏鲁簇 ~2.1 vs 江南/网文 ~1.1,"不可名状/仿佛"招牌腔);
  dialogue_ratio F=4.8;其余 6 维 F<1.5(语言通性噪声)。江南 3 部跨子类型 `sent_len_cv≈0.68` 几乎不变=作者签名。
  发现 `comma_per_kchar` 退化(只数半角,中文全角→失效)。
- 结论:**"低方差=类型基因"表述错**——应是 **between/within F 比高**(簇内稳+簇间分得开),raw 低方差多为语言通性。
  8 结构维里只 hedge 强判别 → **生产指纹太薄、撑不起类型身份;题材灵魂在语义层(意象/修辞/氛围),结构指纹只配 F 高的几维当辅**。
  作者基因确有其物(江南 sent_len_cv 跨子类型恒定)。
- 边界:簇有作者混淆(无同题材不同作者 3+ 本),hedge 分出余烬是题材性还是作者性无法区分。
- 落地:`docs/V1-方差判别器-结论.md`;commit 见本次。下一步 V2(共性提纯是否更套路)。

## 2026-06-28 · V2(#12)共性提纯会不会更套路(live · 存亡问题)

- 操作:`docs/experiments/v2_common_vs_cliche.py`(克苏鲁蒸汽朋克:余烬之铳+深海余烬;3 主题 × A纯共性/B单书/C共性+留白 + 盲排)。
- 结果(3 主题):最套路次数 A=2 / B=1 / C=0;最不套路 A=0 / B=0 / **C=3**;最追读 1:1:1。陈词命中 A=1/B=0/C=0。
- 结论:① **假设成立——裸取最大公约数确实更套路**(A 2/3 最套路、理由"堆砌符号/组合陈旧")→ 纯共性不可裸用。
  ② **留白槽是有效解药**(C 3/3 最不套路)→ 模板必须内建"负面清单 + 强制求异/留白槽"。
  ③ **关键 nuance:"不套路"≠"追读"**(追读打平,C 两次被评节奏慢)→ 新鲜度与节奏正交、会 trade-off,需双护栏。
  ④ 副发现:确定性陈词黑名单太小(判别近无),要廉价度量需扩表。
- 边界:n=3 单轮单裁决,方向性非定论;B 单书也含作者套路。
- 落地:`docs/V2-共性是否更套路-结论.md`、`docs/experiments/v2_results.txt`;commit 见本次。下一步 V3(融合变粥)。

## 2026-06-28 · V3(#3)融合会不会变粥(live)

- 操作:`docs/experiments/v3_fusion_mush.py`(余烬之铳 × 天之炽-江南,2 源;3 主题 × A朴素/B结构化;盲分三桶)。
- 结果:6 段 **0 段"平均粥"**;桶两臂均 {新自洽2, 可辨拼贴1, 平均粥0};coherence ~86、distinct ~84;更好 B结构化2 : A朴素1。
- 结论:① **"变粥"基本没发生**(2 源+现代 LLM 倾向合成连贯新声音)→ brainstorm 强担忧在小源数下不成立。
  ② **真正失败模式是"可辨拼贴/缝感"**(标志意象生硬并置)→ 要防"缝"不是"粥"。
  ③ **结构化融合≈朴素、只略好非决定性** → "必须分轨配额"被弱化,朴素已够用,结构化作可选增强。
- 边界:**仅 2 源、n=3、单裁判**,不能外推;源数是关键变量,3–5 源(真正"杂糅多作者"场景)未测,列 backlog 升级版 V3。
- 落地:`docs/V3-融合是否变粥-结论.md`、`v3_results.txt`;commit 见本次。

## 2026-06-28 · 加入 5 本同题材不同作者(克苏鲁维多利亚)+ V1 重跑(推翻原结论)

- 触发:用户指出诡秘之主/余烬之铳/诡秘地海/黎明医生/深海余烬 同属克苏鲁+维多利亚(我前几验缺的"同题材不同作者"语料)。
- 操作:切分缺失 3 本(诡秘之主1394章/诡秘地海1149/黎明医生779,纯本地不烧token);用 5 不同作者同题材簇重跑 V1。
- **推翻 V1 原头号结论**:hedge F 28.6→**0.44**(原"克苏鲁簇"=余烬+深海疑同源→**假阳性**;5 不同作者 hedge CV=0.27)。
  5 同题材不同作者间 8 结构维**几乎无共性**;江南(同作者)仍大量低 CV(sent_len_cv CV=0.008)。
- 定论:**结构指纹=作者指纹,不是题材指纹;题材身份纯在语义层**。"类型基因 vs 作者基因"分层成立但落点反直觉。
  教训:验题材基因必须"同题材不同作者",同源 2 本造假阳性。
- 硬约束:类型模板**只用语义层**(意象/母题/世界观/语汇),结构指纹归作者层(留给"贴原作者"niche)。
- 落地:`docs/V1-方差判别器-结论.md`(加"重大修正")、`v1b_5authors_results.txt`;commit 见本次。下一步 V_genre(从5本语义抽克味模板看生成够不够克)。

## 2026-06-28 · V_genre 通用类型模板正题(live · 方向成立)

- 操作:`docs/experiments/v_genre_template.py`(5 书语义层抽克苏鲁维多利亚模板;A模板+求异 / B裸prompt / C单作者诡秘之主;3主题盲评克味/套路/新鲜)。
- 结果(均值,套路越低越好):A 克味88.3/套路51.7/新鲜81.7;B 85.0/63.3/66.7;C 86.7/46.7/76.7。最克味次数 A=2/B=0/C=1。
- 结论:① **A 比 B(裸prompt)三轴全胜**(套路↓12、新鲜↑15)→ **模板有真实增量、非靠 LLM 常识,正面回应"过度设计"质疑**。
  ② **A ≥ C(贴单作者)**(克味/新鲜更高)→ **不必贴单作者,通用模板更好**,贴原作者退 niche 成立。
  ③ 抽出的模板本身可交付(意象池/母题/世界观语汇/味道要诀);增量主要在去套路+新鲜(V2 留白生效)。
- 边界:n=3 单裁判、裁判强题材先验(B 克味也 85);克味增量小(+3),真增量在套路/新鲜两轴。
- 落地:`docs/V_genre-通用类型模板-结论.md`、`v_genre_results.txt`;commit 见本次。
- **整轮总判(V1→V_genre)**:用户 reframe 成立、系统有真实增量;方向=可复用/可杂糅/带求异消缝护栏的**语义类型模板**,结构指纹归作者层(贴原作者 niche)。

## 2026-06-28 · V5(#4)跨书检索 vs 单书(live · 反直觉,推翻假设)

- 操作:`docs/experiments/v5_crossbook_retrieval.py`(5 本克苏鲁池 vs 单书余烬之铳;同模板底色+同中文查询,只差检索范围;3主题盲评)。注:初版规划器吐英文查询(对中文FTS无效)已修为强制中文。
- 结果:**单书胜 3/0**(克味92.3 vs 81.7、意象89 vs 82.7、质量90.7 vs 85);单书在主题2/3 **0命中**仍完胜跨书有命中。
- 结论:**跨书 raw 片段注入反而更差**——5 不同作者异质片段互相稀释克味(V3"缝感"在检索层翻版)。真正起效的是**预蒸的连贯类型模板**,非写时多书生检索。→ 推翻 #4;强化 genre_template 必要性。
- 边界:单书多数0命中→实际≈"纯模板",纯净"单vs跨"被弱化;未测"跨书先归一改写再注入"。
- 落地:`docs/V5-跨书检索-结论.md`、`v5_results.txt`。

## 2026-06-28 · genre_template 产品功能(把 V_genre 固化为可保存可调用资产)

- 操作:store.py 加 genre_template 表+CRUD;generate/genre_template.py(extract/render_system_prompt/preview);
  main.py 加 5 端点;tests +2 纯函数单测(回归 25/25)。
- 端到端实测:从 5 本克苏鲁维多利亚抽出「克苏鲁维多利亚」模板并入库,preview 现写够克味段落(值夜者/诅咒人偶/停尸间);
  anti_patterns 顺带实现 V4 负面清单。projects.db 已 gitignore。
- 依据:V_genre(模板>裸prompt)、V1(纯语义)、V2(求异护栏)、V5(离线蒸连贯模板)。
- 落地:`docs/genre_template-产品功能.md`;commit 见本次。下一步:前端页 + 接 compose 注入 + 旋钮(V6)。

## 2026-06-28 · genre_template 前端页(抽取/查看/试写)
- 新增 `app/genre/page.tsx` + lib/api 5 方法 + 5 处 rail 入口。左侧多选同题材书抽取+列表,右侧详情(6 部件+system_prompt)+试写。
- 前端 tsc 0 错;起 :8100 curl /genre-templates 返回已存「克苏鲁维多利亚」(9 意象 / 606 字 system_prompt),HTTP 通。
- 落地:commit 见本次。启动见 `docs/genre_template-产品功能.md`。

## 2026-06-29 · V6(#7)旋钮可控:类型强度 / 求异度滑杆(live 单调性验证 + 功能落地)

- 实现:render_system_prompt 加 genre_strength/novelty 两旋钮(_band 三档渲染),preview/端点按旋钮实时重渲;前端 genre 页加两滑杆。
- 验证(`docs/experiments/v6_knob_monotonic.py`):
  求异度 15→50→85:新鲜 55→65→75(单调↑)、套路 80→70→60(单调↓);类型强度 15→50→85:克味 75→80→85(单调↑)。
  串扰:强度对套路/新鲜无方向性(噪声带内);求异对克味轻微正向(无害)。
- 结论:**旋钮可预测、可微调(#7 通过)**;边界 n=1主题/轴、单裁判,方向可信幅度不精确。
- 落地:`docs/V6-旋钮可控-结论.md`、`v6_results.txt`;回归 26/26;前端 tsc 0 错。commit 见本次。

## 2026-06-29 · 句法/句式层架构审查(workflow 5模块只读 + 评审)

- 触发:用户指出文风不只词汇频率,还有句法/句式(翻译腔/倒装/状语前置 + "主角眼底寒芒一闪"类套路句式 slot 模板);要审查现有架构是否捕捉。
- 操作:workflow 并行审查 style_genome/genre_template/_fingerprint/craft/墨笔style+writer + 综合 + 评审。7 agents。
- 结论:**句法/句式模板层是盲区**。词汇/意象、粗结构统计 ✓;但(A)题材句式/翻译腔、(B)套路句式 slot 模板 几乎全缺。
  **genre_template(新建)结构性缺失**:纯语义、无句法维,且 _distill 把翻译腔随"结构"连坐排除(过度套用 V1)。
  反套路火力全在词汇/短语级,无法泛化(禁"瞳孔骤然收缩"挡不住"眸光猛地一缩")。
- 评审关键修正:① 砍 pos_slots 词性 schema(中文无可靠轻量标注)→ 字符串数组+2真实变体;② 频次是一等公民(套路=高频非布尔);
  ③ 翻译腔按题材选边(克味题材进正向、爽文进负面),非一律消除;④ 化解 V1 张力:**跨作者不变性**判据 + syntactic_patterns 只在 multi=True 抽 + 留一作者检验(把张力变可证伪实验);⑤ 确定性指标只取高特异性,砍通配正则。
- MVP:只动 genre_template.py(cliche_sentence_templates)+ _fingerprint.py(命中频次)。
- 落地:`docs/句法句式层-架构审查与提案.md`、`experiments/句法层审查_raw.txt`;commit 见本次。**未改代码,待确认后建 MVP。**

## 2026-06-29 · 句法/句式层 MVP(代码-测试-验证一条龙;抽取赢、生成注入未验出增量)

- 实现:genre_template 加 syntactic_patterns(题材惯用句式/翻译腔,仅 multi 抽)+ cliche_sentence_templates(套路句式 slot 模板);
  _fingerprint 加确定性句法维(套路同句共现 + 高特异性翻译腔标记);前端展示两字段。
- 抽取实测✅:克苏鲁维多利亚抽出 5 题材句式(状语前置/长定语/西式连接词带原著例句)+ 16 套路句式模板——兑现用户"抽离出来"。
- 验证(n=3 单裁判):
  - V_syntax(in-prompt 注入):**反更差**——套路 40→65、新鲜 82→63,A无句法 3:0 胜。根因:套路句式列入提示=反向 priming。
  - V_syntax2(生成后 linter):**无可测收益**——原稿 vs 去套路稿评分完全相同(克味85/套路70/新鲜63),即便改 238/377 字。
- 决策:句法注入**默认关**(inject_syntax=False),抽取/展示保留;新增 cliche_lint() 作 opt-in。
- 方法论局限:裁判对句子级改写给相同分→分辨率不足;n=3 太小。不能断言"句法层对生成无用",只能说"这套度量没测出增量"。
- 落地:`docs/V_syntax-句法层-结论.md`、v_syntax_results.txt、v_syntax2_results.txt;回归 28/28。
- 下一步候选:A 抽取/分析-only(当前默认,性价比最高)/ B 更灵敏评测重测 / C detector 从模板现编译锚词做硬尺子。倾向 A。
